#include <stdio.h>
/**
 * _islower - checks for lowercase
 *
 * Return: 1 if c is lowercase and 0 otherwise
 */

int _islower(int c)
{
        if (putchar('c') >= 97 || putchar('c') <= 122)
	{
		return 1;
	}
	else
		return 0;
}
