#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
    char v[100] = {"\"Programming is like building a multilingual puzzle\n"};

    puts(v);

    return (0);
}

